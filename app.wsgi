import sys
import os

# Add your project directory to the sys.path
sys.path.insert(0, '/var/www/html/vital-projects/vital-python')

from run import app as application  # Import the Flask app instance

# If your app is named differently, adjust the import as needed
